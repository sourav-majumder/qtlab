====================
QTLab Changelog file
====================

In the recent past, no new version numbers have been assigned.

The HEAD of the "aalto" branch is supposed to be stable.

See the git(hub) history for changes (and forks).
